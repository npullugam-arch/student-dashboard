# Application
spring.application.name=student-portal
server.port=8080

# Supabase PostgreSQL datasource
# Replace the three placeholders below with the values shown in Supabase's
# database connection settings. Keep sslmode=require in the JDBC URL.
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0

spring.datasource.username=postgres.zzzbldtaccxftnovgtpi
spring.datasource.password=Nandu@Minnu
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable SQL script auto-run (for now)
spring.sql.init.mode=never

logging.level.org.springframework.security=TRACE
logging.level.org.springframework.web=INFO

app.upload.answer-sheets-dir=uploads/answer_sheets
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
